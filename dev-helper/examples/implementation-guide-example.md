# Example Development Guide: P1-1 - Spider Service Wrapper

This is a concrete example of a completed guide generated by the `implementation` stage.

Use it only as a formatting and specificity reference. Do not copy its domain details, module names, or acceptance criteria into unrelated tasks.

```markdown
# Development Guide: P1-1 - Spider Service Wrapper

> Generated: 2026-03-14T22:00:00+08:00
> Architect: implementation skill
> Status: Draft -> Ready for development
> Source: dev_spec.md §9.3, docs/testing_strategy.md §12.x

## 1. Task Context

### Scope Boundary
- **Task ID**: P1-1
- **Task Name**: Spider Service Wrapper
- **Phase**: Phase 1 Infrastructure
- **Dependencies**:
  - ✅ P0-1 Git 子模块配置
  - 🔄 P0-4 测试单元准备

### Acceptance Criteria
- [ ] AC1: Return standardized `XHSPost[]`
- [ ] AC2: Implement 3+2 retry with 2/4/8/16/32s backoff
- [ ] AC3: Distinguish Transient/Permanent errors
- [ ] AC4: Write cooldown on 5 failures

### Test Requirements
- **Test File**: `tests/unit/test_spider.py`
- **Test Scenarios**:
  1. First success returns `XHSPost[]`
  2. Three auto-retries then success
  3. User retry stage success
  4. Five failures writes `spider_cooldown_until`
  5. Error classification is correct
  6. Backoff intervals match the spec
- **Test Target**: Lock Spider output contract, retry flow, and cooldown behavior

## 2. Architecture Context

### System Position
```text
[StrategyAgent] -> [XHSSpider] -> [Spider_XHS]
                     |
                     v
              [SessionManager]
```

### Tech Stack
- Language/runtime: Python 3.9+
- Primary libraries/services: Spider_XHS submodule, asyncio
- Execution pattern: wrap sync spider calls via `asyncio.to_thread()`
- Key behavioral constraints: retry windows and cooldown must match the spec

### Constraints
- Auto retry: 3 attempts
- User retry: 2 attempts
- Cooldown: 30 minutes after 5 failures

## 3. Technical Design

### 3.1 Module Structure
```text
app/
  services/xhs_spider.py
  exceptions/spider_exceptions.py
  models/schemas.py
tests/
  unit/test_spider.py
```

### 3.2 Class & Interface Design

**Primary Class Or Entry Point**: `XHSSpiderClient`

```python
class XHSSpiderClient:
    async def search(self, session_id: str, query: str, attempt: int = 0) -> list[XHSPost]:
        ...
```

### 3.3 Algorithm & Logic Flow

**Core Flow**:
```text
check cooldown
  -> call Spider_XHS
  -> classify errors
  -> retry on transient failures
  -> persist cooldown after terminal failure
```

### 3.4 Error Handling Strategy

**Exception Hierarchy Or Failure Mapping**:
```text
SpiderException
├── TransientError
├── PermanentError
├── SpiderCooldownError
└── SpiderServiceUnavailable
```

## 4. Testing Strategy

### 4.1 Test Pyramid Mapping

| Level | File | Count | Focus | Mock Strategy |
|-------|------|-------|-------|---------------|
| Unit | `tests/unit/test_spider.py` | 6 | Retry logic and cooldown | Mock SessionManager and Spider backend |
| Integration | N/A | 0 | N/A | N/A |
| E2E | N/A | 0 | N/A | N/A |

### 4.2 Critical Test Scenarios
1. `test_first_success_returns_standardized_posts`
2. `test_three_auto_retries_then_success`
3. `test_user_retry_stage_success`
4. `test_five_failures_triggers_cooldown`
5. `test_transient_vs_permanent_error_classification`
6. `test_backoff_intervals_correct`

## 5. Implementation Checklist

1. [ ] Create exception classes
2. [ ] Implement the client
3. [ ] Add unit tests
4. [ ] Verify cooldown persistence
5. [ ] Confirm acceptance-criteria mapping
```
